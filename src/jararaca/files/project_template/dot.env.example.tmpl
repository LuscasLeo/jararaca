# Message broker (RabbitMQ). The exchange name is passed as a query parameter.
BROKER_URL=amqp://guest:guest@localhost:5672/?exchange=jararaca

# Broker backend (Redis) used for delayed messages and distributed locks.
BACKEND_URL=redis://localhost:6379

# HTTP server
HOST=0.0.0.0
PORT=8000
